using UnityEngine;

namespace FarmerGetToWork
{
    /// <summary>
    /// ShopScene 전용 확장 지점입니다. 실제 로직은 ShopManager가 담당합니다.
    /// </summary>
    public class ShopSceneController : MonoBehaviour
    {
        private void Start()
        {
            Debug.Log("ShopScene UI 준비 완료");
        }
    }
}
